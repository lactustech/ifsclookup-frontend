This is the Next.js (React) frontend for the ifsclookup.in website.

This app is responsible for Server-Side Rendering (SSR) all pages for maximum SEO benefit.

It works by fetching data from the existing FastAPI backend (https://ifsclookup-api.onrender.com).

This project is deployed on Vercel.